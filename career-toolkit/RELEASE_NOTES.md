# Release Notes

## 1.0.0

- Created a publishable `career-toolkit` bundle.
- Preserved all original skill content and assets.
- Normalized skill folder names:
  - `resume-builder-skill` -> `resume-skill`
  - `behavior-question-skill` -> `bq-skill`
  - `salary-negotiation` -> `salary-negotiation-skill`
- Added top-level routing instructions in `SKILL.md`.
- Added `manifest.json` for package metadata.
- Added `install.ps1` for local installation.
- Added bundle-level README with structure and workflow.


